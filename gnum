#!/bin/sh

echo -n "Most    1000: "
./run.sh -gufm <de/words/1000.txt | tail -1
#echo -n "Oxfords 3000: "
#./run.sh -gufm <de/words/oxford_3000_keys/oxford3000_uniq.txt | tail -1
#echo -n "IELTS   4000: "
#./run.sh -gufm <words/ielts_general_4000/ielts_general_4000.txt | tail -1
#echo -n "TOEFL   5000: "
#./run.sh -gufm <words/toefl_5000.txt | tail -1
echo -n "Google  3000: "
./run.sh -gufm <de/words/3000.txt | tail -1



ls -l de/translations/ | wc -l
ls -l de/words/active/ | wc -l
ls -l de/sound/ | wc -l

if [ "$1" = "-a" ]
then
(
     for f in de/translations/*
     do
         (cat ${f} 2>/dev/null; echo;) | grep -v '(' | grep -v  ') '
     done
) | wc -l | xargs echo -n # remove \n 
echo " without (*), where *=n,v,adj,..."
fi

